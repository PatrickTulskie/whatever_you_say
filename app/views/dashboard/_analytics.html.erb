<h2>Interesting Data</h2>
<table id="dashboard_statistics_table">
	<% WusAnalytics.new(current_user).interesting_statistics.each do |stat,value| %>
	<tr>
		<td><%= stat.titleize %></td>
		<td><%= value %></td>
	</tr>
	<% end %>
</table>